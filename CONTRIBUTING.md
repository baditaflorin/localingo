# Contributing

Thanks for helping improve Localingo.

## Local workflow

1. Install dependencies with `npm install`.
2. Run `make install-hooks`.
3. Use Conventional Commits for commit messages.
4. Run `make test` and `make build` before pushing.

## Pull requests

Keep changes focused, include tests for behavior changes, and avoid committing generated secrets or local environment files.
