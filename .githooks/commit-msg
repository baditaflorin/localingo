#!/usr/bin/env bash
set -euo pipefail

MESSAGE_FILE="$1"
MESSAGE="$(head -n 1 "$MESSAGE_FILE")"

if [[ ! "$MESSAGE" =~ ^(feat|fix|docs|chore|refactor|test|ops|data)(\([a-zA-Z0-9._-]+\))?:\ .+ ]]; then
  echo "Commit message must use Conventional Commits, e.g. feat: add lesson flow" >&2
  exit 1
fi
